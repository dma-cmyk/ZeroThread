rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // 開発初期のためのテストルール
    // 注意: 本番環境では公開鍵署名の検証など適切なセキュリティルールを設定する必要があります
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
